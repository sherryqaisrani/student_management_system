import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

const Duration aAnimationDuration = Duration(seconds: 300);
const Curve aAnimationCurve = Curves.easeInOut;

EdgeInsets aPagePadding = EdgeInsets.symmetric(
  vertical: 16.h,
  horizontal: 16.w,
);

EdgeInsets aInputFieldPadding = EdgeInsets.symmetric(
  vertical: 16.h,
  horizontal: 16.w,
);

EdgeInsets aElevetedButtonPadding = EdgeInsets.symmetric(
  vertical: 18.h,
);

final aPageSpecing1 = SizedBox(
  height: 20.h,
);
final aPageSpecing2 = SizedBox(
  height: 40.h,
);

final aContentPadding = REdgeInsets.all(10);
const aTextStyle = TextStyle(fontWeight: FontWeight.bold);
